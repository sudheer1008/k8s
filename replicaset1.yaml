apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: frontend-matchlabels
spec:
  replicas: 3
  selector: 
    matchLabels: 
      env: dev
  template:
    metadata:
      labels:
        env: dev
        tier: frontend
    spec:
      containers:
        - image: nginx
          name: frontend-container